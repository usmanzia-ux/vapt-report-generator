jinja2>=3.1
openpyxl>=3.1
rich>=13.0
pyyaml>=6.0
# Optional, for PDF output:
# weasyprint>=60.0
# Optional, for Word (.docx) templates / output:
# python-docx>=1.1
# docxtpl>=0.16
# PDF from a Word template also needs LibreOffice:  sudo apt install libreoffice
